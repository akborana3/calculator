{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot hosted on Netlify serverless",
  "scripts": {
    "start": "netlify dev",
    "build": "netlify build",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^4.1.1"
  }
}
